@model IEnumerable<ToyStore.Models.Toys>
@{
}

<table>
    <thead>
        <tr>
            <th>Пользователь</th>
        <th>@Html.DisplayNameFor(m=>m.description)</th>
        <th>@Html.DisplayNameFor(m=>m.toy_name)</th>
        <th>@Html.DisplayNameFor(m=>m.price)</th>
        <th>@Html.DisplayNameFor(m=>m.summa)</th>      
    </tr>
    </thead>
      <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        @foreach(var user in item.user){
                            @foreach (var categ in user.toy)
                            {
                        <td>@Html.DisplayFor(modelItem => user.email)</td>
                        <td>@Html.DisplayFor(modelItem => item.description)</td>
                        <td>@Html.DisplayFor(modelItem => item.toy_name)</td>
                        <td>@Html.DisplayFor(modelItem => item.price)</td>
                        <td>@Html.DisplayFor(modelItem => item.summa)</td>
                        }
                        }
                       
                    </tr>
                }
            </tbody>
   
    



</table>
@ViewBag.toys